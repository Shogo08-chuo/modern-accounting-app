```
npm install
npm run dev
```

`.env.example` をコピーして `.env` を作成し、必要に応じて `DATABASE_URL` / `PORT` / `NEXT_PUBLIC_API_BASE_URL` を設定します。

API に Basic 認証をかける場合は `BASIC_AUTH_USER` と `BASIC_AUTH_PASSWORD` も設定します。

```
open http://localhost:3000
```
